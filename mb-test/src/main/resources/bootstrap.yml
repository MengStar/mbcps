spring:
    application:
        name: mb-test
    cloud:
      config:
        uri: http://${CONFIG_HOST:aliyun.com}:${CONFIG_POST:8888}/
        fail-fast: true # 开启之后可以快速重试
        retry:
          multiplier: 2 # 间隔乘数
          max-interval: 8000 # 最大间隔时间
          max-attempts: 8 # 最大尝试次数
server:
  port: 8123

management:
  security:
    enabled: true






