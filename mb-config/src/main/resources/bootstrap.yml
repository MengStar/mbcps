spring:
    application:
        name: mb-config
    cloud:
      config:
        server:
          git:
            uri: https://github.com/MengStar/mbcps/
server:
  port: 8888

eureka:
  client:
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http\://localhost\:8761/eureka/}
